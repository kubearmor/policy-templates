apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netpol-non-rt-ric-inv-adaptor
  namespace: nonrt-sec-plat
spec:
  podSelector:
    matchLabels: 
      app.kubernetes.io/name: inv-adaptor
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: kong 
    ports:
    - protocol: TCP
      port: 8000
  egress:
  - to:
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: kong
    ports:
    - protocol: TCP
      port: 8000
