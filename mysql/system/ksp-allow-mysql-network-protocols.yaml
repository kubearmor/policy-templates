# KubeArmor is an open source software that enables you to protect your cloud workload at run-time.
# To learn more about KubeArmor visit:
# https://www.accuknox.com/kubearmor/

apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: ksp-allow-mysql-network-protocols
  namespace: default   #change namespace to match your namespace
spec:
  tags: ["MySQL", "Allow policy"]
  message: "network accessed by MySQL binary"
  severity: 1
  selector:
    matchLabels:
      app: mysql      #change label to match your label
  network:
    matchProtocols:
    - protocol: tcp
      fromSource:
      - path: /usr/bin/find
      - path: /usr/bin/mysql
      - path: /usr/sbin/mysqld
    - protocol: udp
      fromSource:
      - path: /usr/sbin/mysqld
  action: Allow
