# CHANGE LOG: Added additional admin directories (/usr/sbin, /usr/local/sbin)

apiVersion: security.kubearmor.com/v1
kind: KubeArmorClusterPolicy
metadata:
  annotations:
    app.accuknox.com/type: harden
  name: maint-tools-access
spec:
  action: Audit
  message: "Detected restricted maintenance tool access attempt"
  process:
    matchDirectories:
      - dir: /sbin/
        recursive: true
      - dir: /usr/sbin/
        recursive: true
      - dir: /usr/local/sbin/
        recursive: true
  selector:
    matchExpressions:
      - key: namespace
        operator: NotIn
        values:
          - kube-system
  severity: 1
  tags:
    - MITRE_TA0003_T1543.002
    - MITRE_TA0004_T1548
    - MITRE_TA0007_T1082
    - MITRE
    - NIST_RS.AN-1
    - PCI_DSS_2.2.2
