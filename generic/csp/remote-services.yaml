apiVersion: security.kubearmor.com/v1
kind: KubeArmorClusterPolicy
metadata:
  name: remote-services
  annotations:
    app.accuknox.com/type: harden
spec:
  action: Audit
  severity: 5
  message: "Detected Access to sensitive remote access files"
  selector:
    matchExpressions:
      - key: namespace
        operator: NotIn
        values:
          - kube-system
  file:
    matchDirectories:
      - dir: /etc/ssh/
        recursive: true
      - dir: /root/.ssh/
        recursive: true
    matchPaths:
      - path: /etc/sudoers
      - path: /etc/hosts.allow
      - path: /etc/hosts.deny
  tags:
    - MITRE_T1021_REMOTE_SERVICES
    - NIST_PR.DS-1
    - PCI_DSS_10.2.3
