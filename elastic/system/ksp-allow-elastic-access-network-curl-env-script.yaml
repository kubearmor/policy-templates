# KubeArmor is an open source software that enables you to protect your cloud workload at run-time.
# To learn more about KubeArmor visit:
# https://www.accuknox.com/kubearmor/

apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: ksp-allow-elastic-access-network-curl-env-script
  namespace: default        #change namespace to match your namespace
spec:
  tags: ["Elastic", "Allow Policy", "Baseline policy"]
  message: "Network accessed by Elastic Binary"
  severity: 1
  selector:
    matchLabels:
      app: elastic     #change label to match your label
  network:
    matchProtocols:
    - protocol: tcp
      fromSource:
      - path: /mnt/elastic-internal/scripts/readiness-probe-script.sh
      - path: /usr/bin/curl
      - path: /usr/bin/env
  action: Allow
