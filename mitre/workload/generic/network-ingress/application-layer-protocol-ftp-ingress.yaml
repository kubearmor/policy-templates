apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "FTP-rule"
spec:
  message: "FTP Incoming Traffic is Denyed"
  endpointSelector:
    matchLabels:
      app: myService        #change your labels here
  ingress:
    - toPorts:
        - ports:
            - port: "20"
              protocol: TCP
            - port: "21"
              protocol: TCP
            - port: "990"
              protocol: TCP
            - port: "69"
              protocol: UDP
