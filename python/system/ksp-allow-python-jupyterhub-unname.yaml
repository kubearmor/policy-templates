# KubeArmor is an open source software that enables you to protect your cloud workload at run-time.
# To learn more about KubeArmor visit:
# https://www.accuknox.com/kubearmor/

apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: ksp-allow-python-jupyterhub-unname
  namespace: default
spec:
  tags: ["PYTHON", "JUPYTERHUB","NOTEBOOK", "API"]
  message: "Alert! Access allowed to uname are only from JupyterHub conda python process."
  severity: 1
  selector:
    matchLabels:
      app: jupyterhub
      chart: jupyterhub-1.2.0
      component: singleuser-server
      heritage: jupyterhub
      hub.jupyter.org/network-access-hub: "true"
      hub.jupyter.org/servername: ""
      hub.jupyter.org/username: io
      release: jupyter-hub
  process:
    matchPaths:
    - path: /usr/bin/uname
      fromSource:
      - path: /opt/conda/bin/python3.9
  action: Allow
