apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: system-file-analysis
spec:
  selector:
      matchLabels:
          {}
  file:
    matchPaths: 
    - path: /etc/passwd
    - path: /etc/shadow
  action:
    Audit
  severity: 4